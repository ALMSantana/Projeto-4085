
def calcular_complexidade_tempo(argumentos):
    caminho_script = argumentos["nome_script"]
    metodo_avaliado = argumentos["metodo_avaliado"]
    id_arquivo = argumentos["id_arquivo"]

    print(f"Analisando o script {caminho_script} e o método {metodo_avaliado} para o arquivo de id: {id_arquivo}...")

    return "Complexidade Alta O(n^2)"

